name: service_docket_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  
  # Firebase dependencies
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4
  
  # Camera and image handling
  camera: ^0.11.0+2
  flutter_image_compress: ^2.3.0
  
  # Path provider for file handling
  path_provider: ^2.1.4
  path: ^1.9.0
  
  # HTTP for web image handling
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true